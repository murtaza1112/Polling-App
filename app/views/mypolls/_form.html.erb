<%= form_with(model: mypoll, local: true) do |form| %>
  <% if mypoll.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(mypoll.errors.count, "error") %> prohibited this mypoll from being saved:</h3>

      <ul>
        <% mypoll.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="login-form">
  <div class="field">
    <%= form.label :category_id %>
    <%= form.select :category_id, Category.all.collect{|a| [a.name,a.id]}, {:include_blank => "Select One" } %>
  
  </div>
  <br>

  <div class="actions">
    <%= form.submit "create",:class=>"logbtn"%>
    
  </div>
<% end %>
